
import '../widgets/hero_widget.dart';

///helps to determine from where widget is calling and show animation if not provided
///default noAnimation is selected
///it works with combination of [CustomHeroWidget]


enum HeroTagType { profile, background, article, noAnimation }
